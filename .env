# Variables de docker-compose.yml (racine du monorepo).
# Copier en .env avant `docker compose up`.

# --- PostgreSQL ---
POSTGRES_DB=casa
POSTGRES_USER=casa
POSTGRES_PASSWORD=casa

# --- Port exposé par le reverse proxy nginx (point d'entrée unique de l'app) ---
APP_PORT=8080
