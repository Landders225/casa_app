# URL de l'API backend, telle que vue par le navigateur (via le reverse proxy nginx).
# En local avec docker-compose, nginx écoute sur APP_PORT (8080 par défaut) et
# route /api/* vers le backend Laravel.
VITE_API_URL=/api
